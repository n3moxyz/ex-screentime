# ex-screentime

Initial project repository scaffold for `ex-screentime`.

## Status

This repo is ready for a first real implementation pass. The application stack, runtime commands, and deployment target are intentionally left TBD until the project direction is chosen.

## Local Setup

```bash
cp .env.example .env.local
```

No dependencies are required yet.
